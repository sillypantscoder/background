# background
A strange platformer puzzle game.

## How to run

In your terminal, type `python3 compiler.py` (or `py compiler.py` on Windows).

## How to play

The instructions are mostly explained in-game. You can move with the arrow keys (jump with up arrow or space). Pressing R restarts the level.
